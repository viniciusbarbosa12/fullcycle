apiVersion: v1
kind: Service
metadata:
  name: postgres-headless
  namespace: meshcommerce
spec:
  clusterIP: None
  selector:
    app.kubernetes.io/name: postgres
  ports:
    - name: tcp-postgres
      protocol: TCP
      port: 5432
      targetPort: postgres

apiVersion: v1
kind: Service
metadata:
  name: postgres
  namespace: meshcommerce
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: postgres
  ports:
    - name: tcp-postgres
      protocol: TCP
      port: 5432
      targetPort: postgres

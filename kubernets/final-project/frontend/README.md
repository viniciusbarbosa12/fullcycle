# Task Management Frontend

React application for the Task Management API.

## Environment

Create a `.env` file if the API runs on a different URL:

```env
VITE_API_BASE_URL=http://localhost:5284
```

## Commands

```powershell
npm install
npm run dev
npm run lint
npm run build
```

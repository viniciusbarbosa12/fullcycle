FROM golang:1.22-alpine AS build

WORKDIR /src

COPY go.mod ./
COPY main.go ./

RUN CGO_ENABLED=0 go build -o /out/multistage-demo .

FROM alpine:3.20

RUN adduser -D app
USER app

COPY --from=build /out/multistage-demo /usr/local/bin/multistage-demo

EXPOSE 8080
CMD ["multistage-demo"]
